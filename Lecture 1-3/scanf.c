#include <stdio.h>

int main ()
{
    int width, height;
    printf("width & height : ");
    scanf("%d %d", &width, &height);
    printf("width = %d , height = %d", width, height);
    return 0;
}
