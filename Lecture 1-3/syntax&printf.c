#include <stdio.h>

int main()
{
    float height, base, area;
    height = 5;
    printf("%.2f\n",height);
    height = 10;
    printf("%.2f\n",height);
    base = 2;
    area = (height*base)/2;
    printf("%.2f\n",area);
    return 0;
}