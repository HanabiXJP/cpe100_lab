#include <stdio.h>
#include <stdbool.h>

int main() {
    // Define a boolean variable
    bool isRaining = false;

    // Check the boolean value and print a message
    if (isRaining) {
        printf("Grab an umbrella!\n");
    } else {
        printf("No need for an umbrella today.\n");
    }

    return 0;
}
